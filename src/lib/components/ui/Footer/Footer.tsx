import React from 'react';

import { clsx } from 'clsx';

export const Footer: React.FC = () => {
  return (
    <footer className={clsx('flex items-center gap-x-10', 'h-16')}>
      Footer
    </footer>
  );
};
